# Copy to MorningPush.Config.ps1 (gitignored) or edit values below in your local copy.

# Leave empty to use the repo root (parent folder of GitHub-Push-Automation).
$RepoPath = ""

$DailyBumpFileName = "daily-fullstop.txt"
